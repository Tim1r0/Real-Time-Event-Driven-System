[project]
name = "real-time-event-driven-system"
version = "0.1.0"
requires-python = ">=3.14,<4"
dependencies = ["fastapi (>=0.141.1,<0.142.0)", "uvicorn[standard] (>=0.52.4,<0.53.0)", "pydantic-settings (>=2.15.0,<3.0.0)", "redis (>=8.1.0,<9.0.0)", "taskiq (>=0.12.4,<0.13.0)", "taskiq-aio-pika (>=0.6.0,<0.7.0)", "taskiq-redis (>=1.2.3,<2.0.0)"]
